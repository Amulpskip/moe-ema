# 背景デザイン（本番適用済み）

参考サイト（https://roriitanononoko.com/ ）の背景構成を、えまの世界観に合わせて再構成したもの。
**2026-09-23 に本番へ適用済み**（ローカルのみ。公開は未実施）。

## 構成

背景は7層でできています。参考サイトのCSSを読んで構成を確認し、同じ考え方で組み直したものです
（コードのコピーはしていません）。

| 層 | 実体 | 役割 |
|---|---|---|
| 1 | `.bgx-photo i` ×3 | 全面の写真。`images/bg1〜3.jpg` を27秒周期でクロスフェード |
| 2 | `.bgx-veil` | 暗幕。上下を濃く、中央を抜いて被写体を浮かせる |
| 3 | `.bgx-grain` | スキャンライン＋周辺減光（ブラウン管風の質感） |
| 4 | `.bgx-hatch` | 画面全面の極薄な斜めハッチ（印刷っぽい質感） |
| 5 | `.bgx-band--1〜3` | 斜め-7度の帯。スクロール速度を変えてパララックス |
| 6 | `.bgx-word` | セクション名の巨大背景タイポ（-8度・透明度8.5%） |
| 7 | `.bg-wrap`（既存） | 夜空グラデ・魔法陣・星。薄い下地として残している |

トップは**丸写真を置かず**、背景写真そのものを主役にして中央に名前を置く構成です。

## ファイル

| ファイル | 役割 |
|---|---|
| `style.css` 末尾「背景デザイン」節 | 上記すべてのCSS |
| `index.html` / `gallery.html` の `.bgx` `.bgx-hatch` | 背景レイヤーのDOM（HTML直書き。JS注入より初期表示が速い） |
| `bg.js` | 巨大背景タイポの差し込みと、バンド／タイポのパララックス |
| `images/bg1.jpg` `bg2.jpg` `bg3.jpg` | 背景写真。`背景1〜3.jpg` のコピー（日本語ファイル名はURLで事故りやすいためリネーム） |

## 調整したいとき

`style.css` の `:root` にある変数を変えるだけで効きます。

| 変数 | 既定 | 効果 |
|---|---|---|
| `--bgx-photo-op` | `.85` | 写真の見え具合。下げると控えめに |
| `--bgx-veil-top` / `-mid` / `-btm` | `.38` / `.14` / `.90` | 暗幕の濃さ。`mid` を上げると写真が沈む |
| `--bgx-zoom-min` / `-max` | `1.02` / `1.34` | **拡大・縮小の幅。差を広げるほど動きが激しくなる** |
| `--bgx-band1-h` / `-band2-h` | `90px` / `40px` | 斜めバンドの太さ |
| `--bgx-word-op` | `.085` | 巨大背景タイポの濃さ |

写真は「1枚目＝拡大 → 2枚目＝縮小 → 3枚目＝拡大」と交互に動きます。
ズームの全行程が表示区間（約7.5秒）に収まるよう組んであるので、倍率を戻す動きは透明な区間で行われ、
目には映りません。

`prefers-reduced-motion: reduce`（OS側で「視差効果を減らす」を有効にしている人）には、
切替もズームも止めて1枚目だけを静止表示します。

## 同時に入れた変更

| 変更 | 内容 |
|---|---|
| リコメンド欄の削除 | `index.html` から `<section id="recommend">` を削除。通し番号を I〜VII に詰め直し |
| フッター注意書きの削除 | アフィリエイト表記の `<p class="footer-disclosure">` を削除 |
| X アカウント | `@MOE_Emachi` → `@Emachi_moe`（`index.html` のリンクと表示、`app.js` の既定値） |
| 表示IDの自動追従 | `app.js` の `renderProfile()` でURLから `@ID` を取り出して表示ラベルも更新 |

### ⚠️ Xリンクは DB の更新が必須

サイトは起動時に Supabase の `profile.message`（JSON）を読み、その中の `twitter_url` で
HTML のリンクを**上書き**します。**DBに古いURLが残っているとそちらが勝ちます。**

どちらかを実行してください（結果は同じ）：

- **かんたん**: サイト右下の 🔑 で運営ログイン → 「✎ プロフィールを編集」
  → 「X(Twitter) URL」を `https://x.com/Emachi_moe` に書き換えて保存
- **SQL**: `fix_twitter_url.sql` を Supabase の SQL Editor に貼って Run

## 残した点・既知の制限

- **トップ写真の差し替え機能がなくなりました。** 丸写真と一緒に「📷 トップ写真を変更」ボタンを
  外したためです。背景写真を運営画面から差し替えたい場合は別途対応が必要です
  （現状は `images/bg1〜3.jpg` を差し替えて再アップロードする運用）。
- `app.js` のリコメンド関連コード（約60行）は残してあります。すべて `if(!wrap) return` 等で
  ガードされていて、HTML側が無くてもエラーは出ません。
- `images/photo1.jpg`（旧トップ写真）はどこからも表示されなくなりました。
  `app.js` の `hero_url` 既定値としてだけ残っています。
- ルートの `背景1.jpg`〜`背景3.jpg` は `images/bg1〜3.jpg` と中身が同じです（約900KB の重複）。
  公開前に消してよいファイルです。
- 検討用に作ったバリエーション A / B / C の切替サンプル（`sample.html` ほか）は、
  `style.css` への統合で二重適用になるため削除しました。
